name: CI/CD

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 'v20.9.0'

      - name: Install dependencies
        run: npm install

      - name: Build and test
        run: npm run test

      - name: Build and bundle
        run: npm run bundle

      - name: Deploy to device/emulator (example)
        run: npm run android
